{
  "name": "piedra-papel-tijera",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "parcel": "^2.12.0"
  },
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /cachipun",
    "deploy": "gh-pages -d build"
  }
}
